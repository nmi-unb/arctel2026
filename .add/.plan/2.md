# Pré
1. Comportamento:
   1. Solicite a não execução de loops de teste por parte da IA quando for algo que o user possa testar manualmente. Somente em caso de erros silenciosos reportados pelo user devem ser executados teste pelo agente.
   2. Ao criar um arquivo `.py` que será invocado por outro, no final dele cite quais funções, variáveis e constantes serão invocadas dentro de `__all__ = []`, funções internas devem começar com `_`, sendo assim sinalizadas como privadas... Se o arquivo não for invocado a primeira linha deve ser `# ignore file`. Na pasta do arquivo invocado normalemnte colocamos um `__init__.py` (mesmo que seja um arquivo vazio), certo?! Continue criado o `__init__.py` **vazio**, temos uma aplicação automatizada que recriará os `__init__.py` de maneira detalhada e adequada...

2. Observações:
   1. [OBRIGATÓRIO] será utilizado UV;
   2. [OBRIGATÓRIO] levar em consideração as skills definidas em `.codex` e `.claude`;
   3. [OBRIGATÓRIO] ao utilizar a biblioteca `Flet` tomar cuiddado com o seguinte:
    """
    ```py
        uv run flet create
    ```
    ### Important
    Any existing README.md or pyproject.toml (for example, created by uv init) will be replaced by the one created by flet create command.

    The command will create the following directory structure:

    ```DOS
        README.md
        pyproject.toml
        src
            assets
                icon.png
            main.py
        storage
            data
            temp
    ```
    You can find more information about flet create command here.

    ### Auto-update
    Flet automatically calls page.update() (or .update() on the nearest isolated ancestor) at the end of every event handler and main() function. This means you don't need to call .update() yourself in most cases:

    ```py
        import flet as ft

        def main(page: ft.Page):
            def button_click(e):
                page.controls.append(ft.Text("Clicked!"))
                # no need to call page.update() — it happens automatically

            page.controls.append(ft.Button("Click me", on_click=button_click))
            # no need to call page.update() here either

        ft.run(main)
    ```
    > Note
    > If your event handler already calls .update() explicitly (e.g. code written for Flet 0.x), the automatic update is skipped to avoid a redundant double update.

    ### Disabling auto-update
    You can disable auto-update for fine-grained control over when updates are sent to the client. Use ft.context.disable_auto_update() and ft.context.enable_auto_update() to toggle the behavior.

    When called inside a handler, the setting applies to the current handler context only:

    ```py
        import flet as ft

        def main(page: ft.Page):
            def add_many_items(e):
                ft.context.disable_auto_update()
                for i in range(100):
                    page.controls.append(ft.Text(f"Item {i}"))
                page.update()  # single update for all 100 items

            page.controls.append(ft.Button("Add items", on_click=add_many_items))

        ft.run(main)
    ```

    When called outside of event handlers (e.g. at the module level), it controls the global default for the entire app:

        ```py
        import flet as ft

        # disable auto-update globally
        ft.context.disable_auto_update()

        def main(page: ft.Page):
            def button_click(e):
                page.controls.append(ft.Text("Clicked!"))
                page.update()  # must call explicitly since auto-update is off

            page.controls.append(ft.Button("Click me", on_click=button_click))
            page.update()

        ft.run(main)
        ```
    """

# Decisões

1. Opção C – 2 FASES: A interface editará avisos.json e permitirá consultar os módulos e aulas em modo somente leitura. A edição dos links dos módulos entra em uma segunda fase

2. Diretório utilizado:
   1. Vamos utilizar uma branch nomeada `maintenance`;
   2. Obs.: Dentro dessa pasta, qualquer pasta que comece com `.` deve ter um carater privado, apenas com a finalidade "explicativa" e não lógica.
   3. Para o controle fino dos caminhos utilizados, vamos utilizar `PATHLIB` e aproveitar para organizar constantes utilizadas em vários pacotes `.py` da aplicação seguindo a estrutura sugerida abaixo:
   ```DOS
        └── services
        │   └── generic
        │   │   ├── paths
        │   │   │   ├── __init__.py
        │   │   │   ├── target.py
        │   │   │   └── root.py
        │   │   ├── values
        │   │   │   ├── __init__.py
        │   │   │   └── constants.py
        │   │   └── __init__.py
    ```

3. Proposta aceita, quanto a organização:
   ```DOS
        └── services
        │   └── dataStructure
        │   │   ├── __init__.py
        │   │   ├── notice.py
        │   │   ├── module.py
        │   │   ├── lesson.py
        │   │   └── LessonLinks.py
    ```

4. Proposta aceita;

5. Proposta aceita;

6. Proposta aceita;

7. Proposta aceita;

8. Proposta aceita, observação:

   - Vamos construir as ações de `Editar` e `Aplicar` ("Salvar arquivo: grava em avisos.json")
   - Nem na FASE 1 nem na FASE 2 haverá o comando `Publicar`.
   - Como declaramos em 2.1, estamos em uma branch de manutenção. Já existem, separadamente, que auxilia o user a fazer o deploy.

9.  Proposta Recusada, como estamos lidando com um projeto instânciado no github e de pequena magnitude é overkill essa feature. Vamos nos limitar a colocar janela de pop-up de confirmação quando o user clicar em ações que sobrescrevam arquivos, solicitando que o mesmo execute um commit antes de prosseguir para salvar o estado anterior...

10. Proposta aceita, siga-se:
    É mais seguro preservar a ordem física e oferecer:

    ```text
        Ordenar por publicação
        Ordenar por prioridade
        Mover para cima
        Mover para baixo
    ```

11. Proposta aceita;

12. Proposta aceita;

13. Proposta aceita;

14. Proposta aceita;

15. Proposta aceita, resalva:
    - Nome do projeto: `.notice_editor` no fim é uma aplicação privada que não pertencer ao corpo do site, mas será apenas uma ferramenta para adicionar informações ao site sem utilizar hardcode...
    - IMPORTANTE: 
      - na raiz do repositório existe o arquivo `tree_focada.txt` que sumariza o projeto em uma tree.
      - o arquivo `_docs\dFilter.py` identifica quais arquivos vão ou não para o *deploy*. Por *default* qualquer pasta cujo nome começa com "." não vai para o *deploy*.

16. Proposta aceita;

# Retorno 

Prompt solicita e estrutura o MPV 1.0;